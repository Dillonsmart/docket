# Build output
/docket
/dist/
*.test

# Coverage and profiling
coverage/
*.out
*.prof

# Editors and OS
.idea/
.vscode/
.DS_Store

# The working plan behind this project is not part of it
docket-project-plan.md

# Claude Code settings carry this machine's paths (docket init writes the hook
# command into them); anything else under .claude/ can be shared
.claude/settings.json
.claude/settings.local.json
